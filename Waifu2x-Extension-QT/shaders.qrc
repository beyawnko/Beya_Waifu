<RCC>
    <qresource prefix="/shaders">
        <!-- qmake should populate this based on the SHADERS variable -->
        <!-- If shaders are not found at runtime, verify .qsb files are generated -->
        <!-- and correctly embedded. Manually list .qsb files here if needed. -->
    </qresource>
</RCC>
